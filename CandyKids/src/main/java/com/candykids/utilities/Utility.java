package com.candykids.utilities;

public class Utility {
	
	/***
	 * This method download the file from web and returns the path where the file will be downloaded.
	 * @param url this is the url containing the resource that we want to download
	 * @return absolute path of the downloaded file.
	 */
	public String downloadFile(String url) {
		return "";
	}
}
